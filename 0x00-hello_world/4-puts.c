#include <stdio.h>
/**
 * Description: main function prints out a string to stdout.
 * Return: at the end 0 if success.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
